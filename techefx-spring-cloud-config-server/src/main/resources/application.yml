spring:
  application:
    name: techefx-spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Andersonfreitas21/techEFX-spring-cloud/environment-variable-repo.git


server:
  port: ${port:8888}
