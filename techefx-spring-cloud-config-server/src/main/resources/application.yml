spring:
  application:
    name: techefx-spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/andersonfreitas21/environment-variable-repo.git


server:
  port: ${port:8888}
